{
  "name": "@bob/queue-consumer-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --local --port 20288 --ip 127.0.0.1 --inspector-port 20831 --persist-to ../../.wrangler-state",
    "lint": "eslint src test --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run test/index.test.ts",
    "test:integration": "vitest run test/smoke.integration.test.ts"
  },
  "dependencies": {
    "@bob/adapters-coderunner": "workspace:*",
    "@bob/core": "workspace:*"
  }
}
